# v6 Phase 3.2 — Overview title refinement

- Keeps the Overview hero title on one line on desktop research layouts.
- Removes the generic 16ch width constraint from the Overview title only.
- Uses a slightly tighter responsive title scale so the line remains editorial rather than oversized.
- Restores wrapping below 820px to prevent document overflow and preserve mobile readability.
- Adds a Phase 3 regression assertion for the one-line desktop title contract.

No scientific model, state, DOI, route, or publication metadata is changed.
